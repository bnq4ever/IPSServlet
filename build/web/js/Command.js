var Command = function () {
    //SERVER STATEMENTS
    var DEVICE_UNKNOWN = 0;

    var DEVICE_CONNECTED = 1;
    var DEVICE_ALLREADY_CONNECTED = 2;

    var DEVICE_DISCONNECTED = 3;
    var DEVICE_ALLREADY_DISCONNECTED = 4;

    var DEVICE_INTRODUCED = 5;
    var DEVICE_ALLREADY_INTRODUCED = 6;

    //DEVICE REQUESTS
    var CONNECT_DEVICE = 7;
    var DISCONNECT_DEVICE = 8;
    var INTRODUCING_DEVICE = 9;
};
